import ReactDOM from "react-dom/client";
import ReactDOM2 from "react-dom";
import { PrimaryButton } from "@fluentui/react";
const defaultDOM = { ReactDOM, ReactDOM2, PrimaryButton };
export default defaultDOM;
